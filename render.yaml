services:
  - type: web
    name: agrilens
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app -w 2 --threads 2 -b 0.0.0.0:$PORT --timeout 120"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.1
